import { FoundationPlaceholder } from '@/features/foundation/foundation-placeholder';

export default function TrainScreen() {
  return (
    <FoundationPlaceholder
      description="El dominio Training se incorporará por verticales en M3."
      title="Entrenar"
    />
  );
}
